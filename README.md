This project analyzes Zepto’s product dataset using SQL to extract business insights.
Key tasks include:

Finding top discounts and premium products

Calculating price per gram to identify best-value items

Computing revenue per category

Checking inventory distribution and out-of-stock patterns

Using DISTINCT, GROUP BY, ORDER BY, SUM, COUNT, ROUND, and filtering conditions

This project highlights strong SQL querying skills and the ability to convert raw data into meaningful insights.
